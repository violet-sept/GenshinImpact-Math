# 复制为 .env.local 后生效（.env* 已被 .gitignore 忽略，不会进仓库）
#
# 这个文件只在**你想覆盖默认行为**时才需要。日常开发与部署都不需要动它。

# ---------- 动态壁纸 ----------
#
# 仓库里**不含**那段壁纸视频：它有 135MB，超过 GitHub 单文件 100MB 的硬上限；
# 画面本身也是《原神》素材，版权情况见 THIRD-PARTY-NOTICES.md。
#
# 默认行为（什么都不配就是这样）：
#
#   本机开发 —— 用 public/wallpaper/nahida.mp4 那个本地文件。
#               离线可用，也不消耗任何外链流量。文件不在也能跑，
#               页面会检测到加载失败并退回纯主题色背景。
#
#   线上部署 —— deploy.yml 里写死了默认地址，指向本仓库 v1.0 Release 的附件：
#               https://github.com/violet-sept/GenshinImpact-Math/releases/download/v1.0/nahida.mp4
#               放在 Release 而不是 Pages 上是有意的：Release 附件不占
#               GitHub Pages 每月 100GB 的软带宽（否则 135MB 一个页面，
#               大约 700 多次访问就会被限流）。
#
# 什么时候需要动这一项：
#
#   · 想换成自己的存储（图床 / CDN / 对象存储）—— 填外链。
#     存储需要允许 Range 请求，否则部分浏览器不会循环播放。
#   · 想彻底不要壁纸 —— 在 CI 里把默认值清空；本地则把 public/wallpaper/ 下的文件删掉。
#     两种情况页面都会退回纯主题色背景，**不会**出现「毛玻璃压在空白上」那种糊掉的效果。
#
# 本地构建填这里：.env.local（本文件另存一份即可）。
# CI 构建填这里：Settings → Secrets and variables → Actions → Variables
#                新建变量 VITE_WALLPAPER_URL —— 工作流用
#                `vars.VITE_WALLPAPER_URL || '<上面那个默认地址>'` 取值，所以它会覆盖默认值。
VITE_WALLPAPER_URL=
